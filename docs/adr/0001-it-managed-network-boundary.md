# Delegate Network Security to IT

The production-like PMU emulator runs only on an IT-managed secure private routed network. It intentionally adds no TLS, client authentication, or simulator-side network access controls, so the emulator can focus on the PDC interoperability contract while IT owns the network security boundary.